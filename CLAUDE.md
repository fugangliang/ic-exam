# CLAUDE.md — IC試験対策アプリ

インテリアコーディネーター一次試験（2026年度・第44回、CBT）対策の本人専用PWA。
要件の正本: `docs/IC試験対策アプリ_要件定義書_v0.3.md`（必ず参照してから作業する）。

## 最重要ルール

- **問題データ（`data/`, `inbox/`, `processed/`）は絶対にgit管理・push しない**（.gitignore済み）。
  市販問題集由来のデータであり私的使用限定。GitHub Pagesはprivateリポジトリでも公開URLになるため、
  デプロイするのはアプリ本体（`app/`）のみ。問題データはアプリのimport機能（F5）で端末に直接投入する。
- 問題データの取り込み・生成では**推測補完禁止**。読み取り不能・根拠不明は必ず人間レビューに回す。
- スケジュール優先度: アプリは手段。「今日から演習を回せる」状態の維持がUI品質より常に優先。

## 構成

```
app/        PWA本体（vanilla JS・ビルド工程なし。index.html/app.js/logic.js/db.js/sw.js）
data/questions/  分野別問題JSON（git対象外）
IC_scan/    スキャン画像の着地先（RFがAirDrop受信後ここに置く・git対象外）
inbox/      未処理の撮影画像（git対象外）
processed/  取込台帳 .ingest_manifest.json＋[要確認]画像のみ（取込確定画像は削除・git対象外）
scripts/    検証・テスト
docs/       要件定義書
```

## 開発ルール

- ビルド工程なし。`logic.js` は純粋関数のみ（DOM/DB非依存、nodeでテスト可能）。
- 変更時は必ず実行: `node scripts/test_logic.js` と `node --check app/*.js`
- 問題JSONを追加したら: `python3 scripts/validate_questions.py data/questions/*.json`
- `app/` 内のファイル構成を変えたら `sw.js` の SHELL リストと VERSION を更新する。
- 動作確認: `python3 -m http.server 8000 -d app` → http://localhost:8000/
- デプロイ: `scripts/deploy.sh`（テスト→main push→app/ を gh-pages に subtree split して push）。
  公開URL: https://fugangliang.github.io/ic-exam/ （publicリポジトリのため docs/ もgit対象外。配信されるのは app/ のみ）

## データ運用フロー（日次: 撮る→取り込む→解く）

**/ingest はSonnet等の高速モデルでの実行を想定**（RF決定 2026-07-13）。品質はモデルではなく
手順書（`.claude/commands/ingest.md`）＋機械チェック（next_ids.py / check_ingest_dupes.py /
validate_questions.py）で担保する。手順の省略・変更をしないこと。

1. iPhoneの書類スキャンで問題＋解説ページを撮影 → **AirDropでMacへ転送し `IC_scan/` に置く**
   （確定: iCloud Drive は使わない。RF決定 2026-07-13）
2. Claude Codeで `/ingest` 実行（画像収集→重複除外→JSON化→検証→台帳登録→画像削除まで一括）。
   重複は二重で防ぐ: 画像は内容ハッシュ台帳（`check_ingest_dupes.py`）、問題は
   `validate_questions.py` のID重複＋問題文重複チェック。
   取込確定した画像は削除する（ディスク節約。台帳にハッシュが残るので再送重複は検出可能）。
   `[要確認]` 問題を含む画像のみ人間レビュー解消まで `processed/` に残す
3. 生成JSONをiPhoneにAirDrop → アプリ⚙「問題データimport」で取り込み
4. **画像・問題JSONはGitHubにアップロードしない**（アプリ本体の更新時のみ `scripts/deploy.sh`）

## 問題JSONスキーマ

```json
{
  "id": "2022-kankyo-001",
  "category": "環境・設備",
  "subcategory": "換気",
  "source": "2022年度 or オリジナル",
  "body": "問題文",
  "choices": ["肢1", "肢2", "肢3", "肢4"],
  "answer": 1,
  "explanation": "正答理由＋各誤答肢がなぜ誤りか",
  "has_figure": false,
  "figure_ref": null,
  "difficulty": 2
}
```
answer は0始まりのindex。import形式は配列 or `{questions:[...]}`。

### 空欄分解形式（過去問題集の穴埋め問題）

過去問題集は「1問につき空欄ア〜エの4箇所、各3択」形式（RF確認・決定 2026-07-13）。
**空欄ごとに1問として分解する**（4空欄→最大4件のJSON、統合しない）。

- **id**: `<出典略号>-<分野略号>-<連番3桁>-<空欄記号>`。空欄記号はア=a/イ=i/ウ=u/エ=e。
  連番は空欄をまたいで共通（例: `kakomon-pla-001-a`〜`-e` は同一問題の4空欄）。
  `next_ids.py` は空欄記号つきIDにも対応済み（連番のみでカウント、空欄違いは増分しない）
- **body**: 問題文全文。**対象の空欄のみ `【　】` を残し、他の空欄は解答・解説ページの正答語で埋めた地の文にする**
- **choices**: 対象空欄の3択（原文のまま。4択に揃えない）
- **source**: `第XX回(YYYY年)第N問-<空欄のカナ>`（例: `第38回(2020年)第1問-ア`）
- **explanation**: 解答・解説ページの当該空欄の解説文。キーワード欄の定義で補って再構成してよい（創作禁止・原文の再構成のみ）
- **has_figure**: 図が無いと解答できない問題のみ true（図は説明の補助に過ぎず本文だけで解ける場合は false。図番号は figure_ref に記録可）
- **difficulty**: 重要度の★の数（★=1 / ★★=2 / ★★★=3）

### 正誤5肢選択形式（「最も不適当なものをN個選べ」等）

まれに「次の記述のうち、最も不適当なものを2つ選びなさい」のような5肢の正誤判定形式が出る
（RF確認 2026-07-14）。ア/イ/ウ/エの空欄はないため、**肢ごとに1問として分解し、
choicesは`["正しい","誤り"]`の2択、answerは解答・解説の正誤判定に従う**。
id接尾辞は a/i/u/e/o（5肢目まで対応、次善不要ならo以降は使わない）。

## フェーズ（要件定義書 §7）

- P1（7/7〜7/14）: 出題モード1,2＋解答履歴＋自信度＋静的解説＋JSON import → **実装済み**
- 7/15〜7/31: 機能追加凍結（バグ修正のみ）。データ制作優先
- P2（8/1〜8/15）: ダッシュボード・弱点集中・SRS・図版
- P3（8/16〜9/15）: 模試モード優先
- 9/16以降: 完全凍結

## 作業ログ（振り返り用）

### 2026-07-21 (2)

**不正解時「選んだ肢の解説」抜粋表示を追加**（RF指示。凍結期間中だがRF明示要望のため対応。
データ改変なし・表示のみ＝7/14の設計判断を維持）:
解説文から選んだ肢の説明部分を抽出する純粋関数 `Logic.choiceExplanationExcerpt` を追加。
一次抽出=肢番号の丸数字（①〜⑤）から次の丸数字までの区間、二次抽出=肢の語を含む文。
解説が選んだ肢に言及していない場合は抜粋なし（創作禁止のため生成はしない）。
実データでの抜粋生成率は誤答肢のべ1,683件中429件（25%。解説の大半が正答中心の記述という
データ特性による）。既存の<mark>強調・「あなたの解答」行は従来どおり併存。
sw.js VERSION: v5 → v6。node全テスト＋headless Chrome E2E（誤答で抜粋表示・正解で非表示・
言及なし解説で非表示・JSエラーなし）検証済み。

### 2026-07-21

**過去問題集後半（7/18スキャン分）の取り込み完了**: `IC_scan/` 投入108画像を9バッチ並列で処理し、
`kakomon_260718.json`（434問）を生成。全体検証は871問・エラー0件。分野内訳:
構造・構法135／住宅設備64／環境工学61／法規61／材料57／表現技法36／照明20。
分野略号 `zai`（材料）・`stb`（住宅設備）・`sho`（照明）を新規追加、環境工学=env・
表現技法=exp・法規=law は既存略号を使用（`.claude/commands/ingest.md` 記載済み）。
形式は既知2種に加え「記述に対して語群から選ぶ」型（空欄なし・記述ごと3択）を確認、
空欄分解と同じ枠組みで肢ごと分解して取り込んだ。
`[要確認]` 3件: `kakomon-zai-014-a`（IMG_3948、本文冒頭の字が不鮮明）、
`kakomon-stb-013-u/e`（IMG_3976、解答・解説欄が写真下端で見切れ・キーワード欄から暫定判定）。
該当2画像は `processed/` に保持。IMG_3900はIMG_3899と同一見開きの重複撮影のため生成対象外
（台帳には登録済み）。取込確定画像はローカル方針（自動許可の制約）によりゴミ箱へ移動
（台帳ハッシュ登録済みのため再送重複は検出可能）。

### 2026-07-14

**過去問題集の取り込み完了**: `IC_scan/` 投入分109画像・全11バッチを処理し、
`kakomon_260713_all.json`（437問）に統合。分野内訳: インテリア計画120／家具81／
各種エレメント56／インテリア販売48／色彩と造形48／インテリアの歴史44／
ウィンドウトリートメント40。分野略号 `iro`（色彩と造形）・`kag`（家具）・
`wnd`（ウィンドウトリートメント）を新規追加（`.claude/commands/ingest.md` 記載済み）。
`[要確認]` 4件（`kakomon-sal-004-a/i/u/e`、IMG_3684由来。本文の類似文言重複を
原本で再確認要、画像は `processed/` に保持）。

**アプリ機能追加**（実運用フィードバックにより、P2予定分を一部前倒しで対応。
DBスキーマ変更なし＝既存 `attempts` 蓄積履歴を集計するロジック追加のみ）:

1. 分野別の得意・不得意統計をホームに表示（正答率／要復習数／総問題数、未着手分野も表示）
2. 出題設定に「直近不正解のみ」「自信なし（迷い・勘）のみ」の絞り込みを追加。
   絞り込み時は間違えた回数（wrongCount）が多い問題を優先
3. 絞り込みなしの通常ランダム出題も、出題回数の少ない問題（未出題含む）を優先し、
   偏り（同じ問題ばかり出る）を防止

sw.js VERSION: v2 → v4。コミット: `3f692a7`（分野略号追加）→ `60afa3d`（統計・絞り込み・
出題バランス追加）→ `d98961f`（総問題数列追加）。デプロイ済み（main・gh-pages とも反映）。

**出題UI改修**（RF指摘: 誤タップから回復できない／不正解時に自分の肢の解説が見つけにくい）:

1. 選択肢は自信度タップ（＝解答確定）まで何度でも選び直せる。確定後は全肢ロック。
   確定ボタンは増やさず自信度タップが確定を兼ねる（タップ数維持）。解答時間は最後に
   選び直した時点までを記録（自信度の熟考時間は含めない、従来仕様を維持）
2. 不正解時、解説冒頭に「あなたの解答: N「…」／正答: M「…」」を表示。さらに選んだ肢の語
   （括弧書き除去、`Logic.explanationHighlightKey`）が解説文中にあれば `<mark>` で強調し、
   自分の肢がなぜ誤りかの説明箇所を見つけやすくした。**データ改変なし・表示のみ**
   （既存437問の解説は誤答肢の説明を地の文に含むため、肢別解説フィールドの追加はしない判断。
   正誤2択の肢「正しい」「誤り」・1文字以下は強調対象外）

sw.js VERSION: v4 → v5。headless Chrome（playwright-core）でE2E検証済み
（選び直し・確定ロック・確定後タップ無効・自信度連打の二重記録なし・正解時は追加表示なし）。
コミット: `378a90d`。デプロイ済み 2026-07-15（main・gh-pages とも反映）。
**7/15より機能追加凍結（バグ修正のみ・データ制作優先）に入る。**
